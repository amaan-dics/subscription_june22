<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <template id="chat_terms_page" name="Chat Terms Popup">
        <t t-call="website.layout">

            <div class="terms-overlay">
                <div class="terms-box">
                    <h4>Terms &amp; Conditions</h4>

                    <div class="terms-content">
                        <t t-raw="terms.content"/>
                    </div>

                    <div class="mt-3">
                        <input type="checkbox" id="accept_terms"/>
                        <label>I agree to Terms &amp; Conditions</label>
                    </div>

                    <button id="accept_terms_btn" class="btn btn-primary mt-3" disabled="disabled">
                        Continue to Chat
                    </button>
                </div>
            </div>

            <script>
                document.addEventListener("DOMContentLoaded", function () {

                    const checkbox = document.getElementById("accept_terms");
                    const btn = document.getElementById("accept_terms_btn");

                    checkbox.addEventListener("change", function () {
                        btn.disabled = !checkbox.checked;
                    });

                    btn.addEventListener("click", async function () {

                        await fetch('/chat/terms/accept', {
                            method: 'POST',
                            headers: {'Content-Type': 'application/json'},
                            body: JSON.stringify({
                                jsonrpc: "2.0",
                                method: "call",
                                params: {}
                            })
                        });

                        // redirect back to chat
                        window.location.href = "/chatbox";
                    });
                });
            </script>

        </t>
    </template>
</odoo>
